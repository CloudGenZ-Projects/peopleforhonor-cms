import { withPayload } from '@payloadcms/next/withPayload'

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configured for local Payload CMS 3.0 API & Admin handling
}

export default withPayload(nextConfig)
